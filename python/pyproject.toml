[project]
name = "memory-sync"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
memory-sync = "memory_sync.cli:main"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

// Package memorysync synchronizes Claude Code sessions across machines.
//
// memory-sync core library: encoding (project-dir naming), rewriter (path rewriting +
// event taxonomy), manifest (checkpoint schema), scanner (regex secret scanner).
// Ported 1:1 from the Python reference at python/memory_sync/.
package memorysync
